/**
 * Provides the classes necessary to create and run threads in a Processing
 * sketch, aka, a PApplet.
 * <p>
 * The framework involves two entities: the PThread and the PThread Manager. The
 * PThread class should be extended with user processing code then instances of
 * these can be added to a thread manager for execution.
 * 
 * @author micycle1
 */
package pthreading;